# PRD: Direction A (Iter 21) - MessageBus 事件驱动架构

## 目标

为 ai-design 引入 **nanobot-style Async MessageBus** 架构，解耦设计事件流，支持多端 Skill 事件广播和异步任务队列。

## 功能需求

### 1. MessageBus 核心
- [ ] **EventBus 类**：基于 `EventEmitter`（Node.js 内置）实现线程安全的内存事件总线
- [ ] **事件类型定义**：`skill_loaded` / `artifact_generated` / `export_completed` / `agent_started` / `agent_completed`
- [ ] **广播订阅**：支持 SSE 端点订阅特定事件类型，支持多订阅者

### 2. Daemon SSE 增强
- [ ] **SSE 事件流**：将 daemon 的 `/api/sse` 改造为基于 MessageBus 的事件发射器
- [ ] **实时推送**：设计 artifact 生成完成、Agent 执行状态变更时主动推送到 Web UI
- [ ] **心跳保活**：30s ping + 心跳计数

### 3. 任务队列
- [ ] **TaskQueue 类**：基于 `Map<string, TaskState>` 的内存任务队列
- [ ] **状态机**：`pending → running → completed / failed`
- [ ] **任务取消**：`DELETE /api/tasks/{id}` 中断正在执行的任务

### 4. Web UI 集成
- [ ] **实时状态面板**：Web Socket 连接显示任务队列状态
- [ ] **事件日志**：显示 MessageBus 事件历史（最近20条）
- [ ] **进度指示器**：SSE 流式显示任务执行进度

## 技术实现

```
EventBus (EventEmitter)
  ├── .publish(event, data)
  ├── .subscribe(event, handler)
  └── .unsubscribe(event, handler)

TaskQueue (Map<id, TaskState>)
  ├── .enqueue(task) → taskId
  ├── .getStatus(taskId)
  ├── .cancel(taskId)
  └── .onComplete(taskId, callback)

MessageBus → SSE → Web UI
```

**技术栈**：
- Node.js `EventEmitter`（原生，无新依赖）
- SSE（Server-Sent Events，原有）
- 无需引入 async queue 库（用 Node.js 原生 events）

## 验收标准

1. `pnpm build` 构建成功
2. Daemon 启动后 SSE 连接建立并收到 `connected` 事件
3. 发布 `test_event` 后 SSE 端收到对应事件数据
4. 任务入队 → 查询状态 → 取消，流程正常
5. **测试通过率 >= 80%**（`NODE_ENV=test npx vitest run --reporter=verbose`）
6. Git commit + push 成功

## 开发分支策略

- `main`: 稳定版本
- `feature/messagebus`: MessageBus 迭代分支
- `gh-pages`: GitHub Pages 部署分支