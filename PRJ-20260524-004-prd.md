# ai-design PRD — Direction A (Iter 2): Agent集成 + BYOK代理 + 多Agent协作

## 项目概述

**项目名称**: ai-design  
**当前阶段**: Direction A - Iter 2（基于前序迭代的新一轮基础架构）  
**参考项目**: YeLuo45/open-design  
**核心功能**: Agent运行时集成 + BYOK API代理 + 多Agent协作工作流

## Direction A (Iter 2) 目标

### 1. Agent 运行时集成

- [ ] **Agent Spawn**: 在项目目录中启动 Agent CLI 进程
- [ ] **实时 Todo 面板**: 显示 in_progress → completed 状态更新
- [ ] **会话管理**: Agent 对话历史持久化到 SQLite
- [ ] **命令转发**: Web UI → Daemon → Agent 的命令通道

### 2. BYOK API 代理

- [ ] **代理端点**: `/api/proxy/{provider}/stream`
- [ ] **支持提供商**: anthropic, openai, azure, google, ollama
- [ ] **自定义端点**: 支持粘贴 baseUrl + apiKey
- [ ] **SSRF 防护**: 阻止内部 IP 访问（10.x, 172.16.x, 192.168.x）
- [ ] **流式响应**: SSE 流式传输 AI 响应

### 3. 多Agent协作

- [ ] **Agent 编排**: 支持多个 Agent 同时运行
- [ ] **任务分发**: 根据 skill 类型分配给不同 Agent
- [ ] **结果聚合**: 合并多个 Agent 的输出
- [ ] **冲突解决**: 处理并发写入冲突

### 4. 协作工作流

- [ ] **设计模式**: design / marketing / operation / engineering / product / finance / hr / sale / personal
- [ ] **模式选择器**: prototype 模式（27技能）vs deck 模式（4技能）
- [ ] **场景分类**: 根据场景自动推荐 Agent 组合

## 技术栈

| 层级 | 技术选择 |
|------|---------|
| 前端 | React 18 + Vite + TypeScript |
| 后端 Daemon | Node.js + TypeScript |
| 持久化 | better-sqlite3 |
| API 代理 | 统一代理层 |
| Agent 运行时 | child_process spawn |

## 验收标准

1. Web UI 能选择并启动一个 Agent
2. Agent 输出能实时流式显示在 Web UI
3. API 代理能成功转发请求到 AI 提供商
4. 自定义 baseUrl + apiKey 配置有效
5. SSRF 防护正确阻止内部 IP
6. 多 Agent 能同时运行并输出结果

## 开发分支

- 开发分支: `feature/agent-byok`（从 feature/skills-enhancement 创建）
- 合并后: `master`
- 部署分支: `gh-pages`