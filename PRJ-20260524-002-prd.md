# ai-design PRD — Direction B: Daemon运行时 + Agent检测 + SQLite持久化

## 项目概述

**项目名称**: ai-design  
**项目类型**: 开源 AI 设计系统  
**当前阶段**: Direction B（迭代自 Direction A）  
**参考项目**: YeLuo45/open-design  
**核心功能**: Daemon 运行时 + 多 Coding Agent CLI 检测 + SQLite 持久化

## Direction B 目标

### 1. Daemon 运行时

- [ ] **进程管理**: Node.js daemon 进程，支持 start/stop/status/logs 命令
- [ ] **pnpm tools-dev**: 一键启动 daemon + web 前端
- [ ] **SSE 流式输出**: 支持 Server-Sent Events 实时推送任务进度
- [ ] **健康检查**: `/health` 端点返回 daemon 运行状态

### 2. Agent 检测

- [ ] **PATH 扫描**: 自动检测 PATH 上的 Coding Agent CLI
- [ ] **Agent 列表**: 显示检测到的 agents（claude, codex, hermes, cursor 等）
- [ ] **运行时 Spawn**: 在项目目录中启动 Agent CLI 进程
- [ ] **Todo 面板**: 实时显示 in_progress → completed 状态

### 3. SQLite 持久化

- [ ] **数据库**: SQLite 保存 projects, conversations, messages, tabs
- [ ] **项目目录**: 每个项目独立的 `.ai-design/` 数据目录
- [ ] **CRUD**: 项目创建/读取/更新/删除

### 4. API 代理（BYOK）

- [ ] **代理端点**: `/api/proxy/{provider}/stream`
- [ ] **支持提供商**: anthropic, openai, azure, google, ollama
- [ ] **自定义端点**: 支持粘贴 baseUrl + apiKey
- [ ] **SSRF 防护**: 阻止内部 IP 访问

## 技术栈

| 层级 | 技术选择 |
|------|---------|
| 前端 | React 18 + Vite + TypeScript |
| 后端 Daemon | Node.js + TypeScript |
| 持久化 | better-sqlite3 |
| 包管理 | pnpm |

## 验收标准

1. `pnpm tools-dev` 能成功启动 daemon + web
2. Web UI 能加载并显示欢迎界面
3. PATH 上有至少一个 Coding Agent CLI 时能被正确检测
4. Daemon 状态可通过 `/health` 端点查询
5. SQLite 数据库正确存储项目数据

## 开发分支

- 开发分支: `feature/daemon-agent`（从 master 创建）
- 合并后: `master`
- 部署分支: `gh-pages`