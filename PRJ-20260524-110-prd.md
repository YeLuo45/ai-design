# PRD: ai-design Direction A (Iter 22)

## 1. 概述与目标

在 Iter 21 MessageBus 基础上，向前推进一步：
1. **Multi-Workspace 支持** — 多个设计工作区切换，数据隔离
2. **Persistent Event Store** — 事件历史从内存改为 LocalStorage 持久化，支持页面刷新后保留

## 2. 功能详情

### 2.1 Multi-Workspace
- 创建/切换工作区（localStorage key: `ai-design-workspace-{id}`）
- 每个 workspace 独立：EventBus history、TaskQueue、UI 状态
- 默认工作区 `default`，支持创建新工作区
- 工作区切换时清空 UI 并重新加载对应状态

### 2.2 Persistent Event Store
- EventBus history 改为 localStorage 持久化（key: `ai-design-events-{workspaceId}`）
- 容量从 max 20 提升到 max 500
- 页面刷新后自动恢复事件历史
- 新增 `clearHistory()` 方法

### 2.3 EventBus API 扩展
```typescript
interface EventBus {
  publish(event: string, data: any): void
  subscribe(event: string, fn: Function): () => void
  getHistory(workspaceId?: string): Array<{event: string, data: any, timestamp: number}>
  clearHistory(workspaceId?: string): void
}
```

### 2.4 TaskQueue API 扩展
```typescript
interface TaskQueue {
  enqueue(data: object, workspaceId?: string): string
  getStatus(taskId: string): object | null
  setStatus(taskId: string, status: string): boolean
  cancel(taskId: string): boolean
  listTasks(workspaceId?: string): object[]
  clearAll(workspaceId?: string): void
}
```

## 3. 技术方案

- **存储**: localStorage（无新依赖）
- **EventBus**: 扩展 history 存储逻辑，写入 localStorage
- **TaskQueue**: 扩展 workspace 逻辑
- **Web UI**: 工作区切换下拉框 + 历史管理按钮

## 4. 验收标准

- [ ] 工作区切换后 UI 正确更新
- [ ] 刷新页面后事件历史保留
- [ ] `clearHistory()` 正确清空历史
- [ ] TaskQueue workspace 隔离
- [ ] 测试通过率 ≥ 80%